import { Routes } from '@angular/router';
import { TestTextImageComponent } from './test-text-image.component';

export const routes: Routes = [
  {
    path: '',
    component: TestTextImageComponent,
  },
];
